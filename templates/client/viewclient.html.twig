{% extends 'base.html.twig' %}

{% block title %}Ajouter un client{% endblock %}

{% block body %}
<h2> {{client.nom}} </h2>
<p style="text-align: center"><br>
{{client.Email}}
<br>
{{client.Telephone}}
<br>
{{client.Adresse}}, {{client.codePostal}}, {{client.ville}}</p>
<div class="column" style="text-align: center">
    <strong>Documents associés</strong>
    {% for document in documents %}
        {{document.numero}}-{{document.client.nom}}
        <br>
    {% endfor %}
</div>
<td><a href="{{ path('app_client_edit', {'name': client.nom}) }}" class="sm-button">Modifier</a></td>

{% endblock %}